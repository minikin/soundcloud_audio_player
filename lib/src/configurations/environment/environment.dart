// library environment;

// part 'environment.g.dart';

// // ignore: non_constant_identifier_names, type_annotate_public_apis
// // Environment Current;

// abstract class Environment<T>
//     implements Built<Environment<T>, EnvironmentBuilder<T>> {
//   Environment._();
//   factory Environment([void Function(EnvironmentBuilder<T>) updates]) =
//       _$Environment<T>;
// }
