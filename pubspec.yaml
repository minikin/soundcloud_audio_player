name: audio
description: SoundCloud Style Audio Player for Flutter.

version: 0.0.6+1

environment:
  sdk: ">=2.8.0-dev <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  audioplayers: ^0.15.1
  built_collection: ^4.3.2
  built_value: ^7.0.9
  cupertino_icons: ^0.1.3
  flutter_bloc: ^3.2.0

dev_dependencies:
  build_runner: ^1.7.3
  built_value_generator: ^7.0.9
  pedantic: ^1.9.0

flutter:
  uses-material-design: true

  assets:
    - assets/data/tunes.json
